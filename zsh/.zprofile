#!/bin/zsh

##
# Because most graphical Linux terminals do not start login shells (unless manually configured to
# do so), .zprofile and .zlogin won't get sourced and have thus effectively been deprecated.
#
